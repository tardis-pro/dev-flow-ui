# Decisions
- Use existing vitest setup with path aliasing
- Mock CF-specific types (KVNamespace, D1Database) at module level
- Static analysis pattern for D1 queries reads source directly
- Tests focus on behavior, not implementation details
